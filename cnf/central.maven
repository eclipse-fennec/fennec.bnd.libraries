# Intentionally empty: the library projects in this workspace are
# resource-only and build without third-party dependencies.
# Add GAVs here if a project ever needs a buildpath dependency, then
# regenerate DEPENDENCIES with tools/dash-licenses.sh.
